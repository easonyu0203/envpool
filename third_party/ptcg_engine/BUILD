# Package marker for the ptcg_engine.BUILD file referenced by the
# new_local_repository("ptcg_engine") rule in //WORKSPACE. Mirrors the
# third_party/<dep>/<dep>.BUILD + BUILD pattern used for every other vendored
# C/C++ library in this tree (e.g. third_party/concurrentqueue/).
